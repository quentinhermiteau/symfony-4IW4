{% extends "base.html.twig" %}

{% block body %}
    <h1>Liste articles:</h1>

    {% for article in articles %}
        <p>{{article.title}}</p>
        <p>{{article.content}}</p>
        ---
    {% endfor %}
{% endblock %}